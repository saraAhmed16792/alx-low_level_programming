#include "main.h"
/**
 * string_toupper - changes all lowercase letters of a string to uppercase.
 * @n: pointer
 * Return: n
 */
char *string_toupper(char *n)
{
	int = i;

	for (i = 0; n[i] != '\0'; i++;)
	{
		if (s[i] >= 'a' && s[i] <= 'z')
			s[i] = s[i] - 32;
	}

        return (n);
}
